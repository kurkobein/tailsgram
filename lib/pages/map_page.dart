import 'package:flutter/material.dart';

class PaginaMapa extends StatelessWidget {
  const PaginaMapa({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('mapa', style: TextStyle(fontSize: 30),),
    );
  }
}