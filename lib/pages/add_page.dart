import 'package:flutter/material.dart';

class PaginaSubir extends StatelessWidget {
  const PaginaSubir({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('subir post', style: TextStyle(fontSize: 30),),
    );
  }
}