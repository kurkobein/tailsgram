import 'package:flutter/material.dart';

class PaginaCalendario extends StatelessWidget {
  const PaginaCalendario({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('calendario', style: TextStyle(fontSize: 30),),
    );
  }
}