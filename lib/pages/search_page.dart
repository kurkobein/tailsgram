import 'package:flutter/material.dart';

class PaginaBuscar extends StatelessWidget {
  const PaginaBuscar({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('buscar', style: TextStyle(fontSize: 30),),
    );
  }
}